# 可配置多智能体决策辅助系统 — 技术文档

> **版本**: 1.0.0 | **更新**: 2026-05-30 | **总代码量**: ~4,600 行

---

## 一、项目概述

### 1.1 产品定位

一个面向复杂问题分析的**多智能体决策辅助工具**。用户输入决策问题后，可选择 2~5 个具有不同人设的 AI Agent（基于金木水火土五行人格），系统并发调用大模型从不同角度分析，并通过语义相似度、冲突检测、评分矩阵和可视化生成综合建议。

### 1.2 技术栈

| 层级 | 技术选型 |
|------|---------|
| Web 框架 | FastAPI (异步) |
| 数据库 | SQLite + SQLAlchemy 2.0 (ORM) + aiosqlite (异步驱动) |
| 数据校验 | Pydantic V2 |
| 鉴权 | JWT (HS256) + bcrypt (passlib) |
| AI 调用 | httpx → DeepSeek-V4-Flash (OpenAI 兼容 API) |
| 语义向量 | sentence-transformers (all-MiniLM-L6-v2) |
| 数值计算 | numpy + scipy |
| CLI 客户端 | rich (终端 UI 框架) |

---

## 二、项目目录结构

```
jixia-Python/
├── os.env                        # API Key（不入库）
├── prompt.md                     # 开发阶段的 Prompt 需求文档
├── Muti-agent PRD.pdf            # 完整 PRD（不入库）
├── 技术文档.md                    # 本文档
├── .gitignore
│
└── backend/                      # 后端项目根目录
    ├── main.py                   # FastAPI 应用入口
    ├── cli.py                    # 终端交互测试客户端
    ├── requirements.txt          # Python 依赖
    ├── app.db                    # SQLite 数据库（不入库）
    │
    ├── core/                     # 核心配置与安全
    │   ├── config.py             # 全局配置（JWT、DB、API）
    │   └── security.py           # 密码哈希、Token 生成/解析
    │
    ├── db/                       # 数据库层
    │   ├── database.py           # 异步引擎、会话工厂、基类
    │   ├── models.py             # 9 个数据表 ORM 模型
    │   └── init_data.py          # 启动初始数据（管理员 + 模板）
    │
    ├── schemas/                  # Pydantic 请求/响应模型
    │   ├── user.py               # 用户认证相关 Schema
    │   ├── task.py               # 决策任务相关 Schema
    │   ├── feedback.py           # 用户反馈相关 Schema
    │   ├── template.py           # Agent 模板相关 Schema
    │   └── admin.py              # 管理员后台相关 Schema
    │
    ├── ai/                       # AI 核心模块
    │   ├── prompt_builder.py     # System Prompt 动态生成
    │   ├── agent_core.py         # 大模型调用 + 后台流水线
    │   ├── similarity_core.py    # 语义相似度计算
    │   ├── conflict_core.py      # 评分冲突检测
    │   └── summary_core.py       # 综合建议生成
    │
    └── api/                      # API 层
        ├── dependencies.py       # 依赖注入（get_current_user/admin）
        └── endpoints/
            ├── auth.py           # 注册/登录/当前用户 (3 接口)
            ├── tasks.py          # 任务创建/状态/结果 (3 接口)
            ├── feedback.py       # 反馈提交/统计 (2 接口)
            ├── history.py        # 历史列表/报告导出 (2 接口)
            ├── templates.py      # 模板查询 (1 接口)
            └── admin.py          # 管理员后台 (7 接口)
```

---

## 三、数据库模型

### 3.1 模型总览（9 张表）

```
users ──1:N──> decision_tasks ──1:N──> task_agents
                      │                       │
                      ├──1:N──> agent_outputs ─┤ (task_agent_id)
                      ├──1:N──> similarity_results
                      ├──1:N──> conflict_results
                      └──1:N──> user_feedback
                      
agent_templates        (独立表，管理员维护)
operation_logs         (独立表，记录系统事件)
```

### 3.2 表结构

#### users（用户表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, 自增 | 用户唯一标识 |
| username | TEXT | UNIQUE, NOT NULL | 用户名 |
| password_hash | TEXT | NOT NULL | bcrypt 哈希密码 |
| role | TEXT | 默认 'user' | 角色：user / admin |
| created_at | DATETIME | 自动生成 | 注册时间 |

#### decision_tasks（决策任务表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, 自增 | 任务唯一标识 |
| user_id | INTEGER | FK → users.id | 所属用户 |
| question | TEXT | NOT NULL | 决策问题 |
| decision_mode | TEXT | NOT NULL | multi_angle / debate / expert_consult / risk_review |
| agent_count | INTEGER | NOT NULL | Agent 数量 (2~5) |
| weight_config | TEXT | 可空 | 用户权重 JSON |
| status | TEXT | 默认 'pending' | pending → processing → completed / failed |
| error_message | TEXT | 可空 | 异常信息 |
| final_summary | TEXT | 可空 | 大模型生成的综合建议 |
| created_at | DATETIME | 自动生成 | 创建时间 |

#### task_agents（Agent 配置表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, 自增 | |
| task_id | INTEGER | FK → decision_tasks.id | 所属任务 |
| agent_name | TEXT | NOT NULL | Agent 名称 |
| role_description | TEXT | 可空 | 角色/专业背景 |
| focus_area | TEXT | 可空 | 关注领域 |
| tone | TEXT | 可空 | 输出风格 |
| final_prompt | TEXT | NOT NULL | 生成的完整 System Prompt |

#### agent_outputs（Agent 输出表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, 自增 | |
| task_id | INTEGER | FK → decision_tasks.id | |
| task_agent_id | INTEGER | FK → task_agents.id | |
| output_text | TEXT | 可空 | Agent 自然语言分析 |
| score_json | TEXT | 可空 | 六维评分 JSON |
| created_at | DATETIME | 自动生成 | |

#### similarity_results（相似度结果表）

| 字段 | 类型 | 说明 |
|------|------|------|
| agent_id_1 | FK → task_agents.id | Agent A |
| agent_id_2 | FK → task_agents.id | Agent B |
| similarity | REAL | 余弦相似度 (0~1) |
| explanation | TEXT | 中文解释 |

#### conflict_results（冲突检测表）

| 字段 | 类型 | 说明 |
|------|------|------|
| dimension | TEXT | 维度 key (benefit/cost/risk/tech/exec/long_term) |
| max_score | REAL | 该维度最高分 |
| min_score | REAL | 该维度最低分 |
| conflict_level | TEXT | high (差值≥4) / low |
| explanation | TEXT | 中文解释 |

#### user_feedback（用户反馈表）

| 字段 | 类型 | 说明 |
|------|------|------|
| chosen_type | TEXT | agent / summary / none |
| chosen_agent_id | FK, 可空 | 采纳的 Agent ID |
| comment | TEXT | 用户备注 |

#### agent_templates（Agent 模板表）

| 字段 | 类型 | 说明 |
|------|------|------|
| name | TEXT, UNIQUE | 模板名称（如"金·决断型"） |
| role_description | TEXT | 职业背景 |
| focus_area | TEXT | 关注领域 |
| tone | TEXT | 输出风格 |
| is_active | INTEGER | 1=启用, 0=禁用 |

#### operation_logs（操作日志表）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | FK, 可空 | 操作用户 |
| event_type | TEXT | 事件类型 |
| description | TEXT | 事件描述 |

---

## 四、API 接口文档

### 4.1 认证模块 (`/api/auth`)

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/register` | 无 | 注册新用户 |
| POST | `/login` | 无 | 登录，返回 JWT Token |
| GET | `/me` | Bearer Token | 获取当前用户信息 |

### 4.2 决策任务 (`/api/tasks`)

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/create` | 用户 | 创建任务 + 触发后台流水线 |
| GET | `/{id}/status` | 用户 | 轮询任务状态 |
| GET | `/{id}/result` | 用户 | 获取完整结果（含 Agent 输出、相似度、冲突、综合建议） |

### 4.3 用户反馈 (`/api/feedback`)

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/vote` | 用户 | 提交采纳反馈 |
| GET | `/stats` | 管理员 | 查看反馈统计 |

### 4.4 历史记录 (`/api/history`)

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/` | 用户 | 当前用户的任务列表 |
| GET | `/{id}/export` | 用户 | 导出 Markdown 报告下载 |

### 4.5 模板管理 (`/api/templates`)

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/` | 用户 | 获取启用的模板列表 |
| GET | `/all` | 用户 | 获取全部模板（管理员可见未启用） |

### 4.6 管理员后台 (`/api/admin`)

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/users` | 管理员 | 所有用户列表 |
| GET | `/tasks` | 管理员 | 全站任务列表 |
| GET | `/stats` | 管理员 | 全局数据看板 |
| POST | `/templates` | 管理员 | 创建模板 |
| PUT | `/templates/{id}` | 管理员 | 更新模板 |
| DELETE | `/templates/{id}` | 管理员 | 删除模板 |
| GET | `/logs` | 管理员 | 操作日志查询 |

---

## 五、核心业务流程

### 5.1 后台分析流水线

```
POST /api/tasks/create
  │
  ├─→ 写入 DecisionTask (status=pending) + TaskAgent 配置
  ├─→ BackgroundTasks.add_task(process_task_background, task_id)
  └─→ 立即返回 task_id

--- 后台异步（独立 DB Session）---

process_task_background(task_id):
  │
  ├─ [1] status → "processing"
  │
  ├─ [2] asyncio.gather([
  │       call_agent_safe(agent1),  ──→ DeepSeek API
  │       call_agent_safe(agent2),  ──→ DeepSeek API
  │       ...                         (每个独立 try/except)
  │     ])
  │
  ├─ [3] 结果 → AgentOutput 表 (逐条写入)
  │
  ├─ [4] calculate_similarities()  ──→ sentence-transformers 向量化
  │     └─→ SimilarityResult 表         两两余弦相似度
  │
  ├─ [5] detect_conflicts()        ──→ 解析 score_json
  │     └─→ ConflictResult 表          维度 max-min >= 4 → high
  │
  ├─ [6] generate_final_summary()  ──→ DeepSeek API (Summary Prompt)
  │     └─→ DecisionTask.final_summary
  │
  └─ [7] status → "completed"
```

### 5.2 Prompt 生成策略

```
Agent 配置 (名称+角色+关注+风格)
  + 决策模式上下文 (multi_angle/debate/expert_consult/risk_review)
  + 用户问题
  + 六维评分维度说明
  + 固定格式要求
────────────────────────────────────────
  → 完整 System Prompt (~1000 chars)
```

### 5.3 六维评分体系

| 维度 key | 中文名 | 含义 (1=最低, 10=最高) |
|----------|--------|----------------------|
| benefit | 收益潜力 | 方案可能带来的正面收益 |
| cost | 成本可控性 | 资金/时间/人力投入是否可控 |
| risk | 风险可控性 | 失败风险和负面后果是否可控 |
| tech | 技术可行性 | 技术上是否可实现 |
| exec | 执行可行性 | 组织层面是否容易落地 |
| long_term | 长期价值 | 是否具备长期战略价值 |

### 5.4 五行人格 Agent 模板

| 五行 | 名称 | 人格 | 职业 | 风格 |
|------|------|------|------|------|
| 金 | 金·决断型 | 刚毅果断，重原则规则 | 法务风控专家 | 严谨型 |
| 木 | 木·生长型 | 生长进取，善于创新 | 产品创新专家 | 鼓励型 |
| 水 | 水·智慧型 | 智慧灵动，深谋远虑 | 战略分析专家 | 中立型 |
| 火 | 火·行动型 | 热情奔放，行动为先 | 项目执行专家 | 激进型 |
| 土 | 土·稳健型 | 稳重务实，兼容并包 | 财务成本专家 | 保守型 |

---

## 六、部署与运行

### 6.1 环境准备

```bash
cd backend
pip install -r requirements.txt
```

### 6.2 配置 API Key

在项目根目录创建 `os.env`：

```
DEEPSEEK_API_KEY=your_api_key_here
```

也可通过环境变量指定自定义 API 地址：

```
DEEPSEEK_BASE_URL=https://your-api.com/v1
DEEPSEEK_MODEL=your-model-name
```

### 6.3 启动服务

```bash
cd backend
python main.py
# API 文档: http://localhost:8000/docs
# 默认管理员: admin / admin123456
```

### 6.4 启动 CLI

```bash
cd backend
python cli.py
```

---

## 七、API 调用示例

### 注册并登录

```bash
# 注册
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"demo","password":"123456"}'

# 登录（获取 Token）
TOKEN=$(curl -s -X POST http://localhost:8000/api/auth/login \
  -d "username=demo&password=123456" | jq -r .access_token)
```

### 创建决策任务

```bash
curl -X POST http://localhost:8000/api/tasks/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "question": "是否应该开发校园二手交易小程序？",
    "decision_mode": "multi_angle",
    "agent_count": 2,
    "agents": [
      {"agent_name":"木·生长型","role_description":"产品创新专家","tone":"鼓励型"},
      {"agent_name":"金·决断型","role_description":"法务风控专家","tone":"严谨型"}
    ]
  }'
# 返回: {"task_id":1, "status":"pending", "message":"任务已提交..."}
```

### 轮询状态

```bash
curl http://localhost:8000/api/tasks/1/status \
  -H "Authorization: Bearer $TOKEN"
# 返回: {"task_id":1, "status":"processing"}
# 等待... → {"task_id":1, "status":"completed"}
```

### 获取结果

```bash
curl http://localhost:8000/api/tasks/1/result \
  -H "Authorization: Bearer $TOKEN"
# 返回: task 信息 + agents + outputs + similarities + conflicts + final_summary
```

### 导出报告

```bash
curl http://localhost:8000/api/history/1/export \
  -H "Authorization: Bearer $TOKEN" \
  -o report.md
```

### 提交反馈

```bash
curl -X POST http://localhost:8000/api/feedback/vote \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"task_id":1,"chosen_type":"agent","chosen_agent_id":1,"comment":"这个角度很有启发"}'
```

---

## 八、开发阶段回顾

| 阶段 | 内容 | 文件数 |
|------|------|--------|
| **Phase 1** | FastAPI 骨架 + SQLite + JWT 鉴权 + 注册/登录 | 9 个文件 |
| **Phase 2** | 决策任务创建 + Agent 配置 + asyncio 并发 LLM 调用 + 后台任务 | +5 个文件 |
| **Phase 3** | 相似度计算 + 冲突检测 + 综合建议生成 + 用户反馈 + 流水线串联 | +5 个文件 |
| **Phase 4** | 历史记录 + Markdown 报告导出 + 模板管理 + 管理员后台 + CLI 客户端 | +7 个文件 |
